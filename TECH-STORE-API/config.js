// Экспортируем объект с конфигурацией приложения
module.exports = {
  // Настройки сервера
  PORT: process.env.PORT || 3000, // Порт сервера: берется из переменной 
  // окружения или 3000 по умолчанию

  // Настройки пагинации (постраничного вывода)
  // Количество элементов на одной странице (максимум и по умолчанию)
  ITEMS_PER_PAGE: 10, 
  MAX_ITEMS_PER_PAGE: 20,

  // Список допустимых статусов заказа
  ORDER_STATUSES: [
    "new",
    "processing",
    "ready_for_pickup",
    "completed",
    "canceled"
  ],

  // Префикс для всех эндпоинтов API
  API_PREFIX: "/api",

  // Учетные данные администратора для базовой аутентификации
  ADMIN_CREDENTIALS: {
    username: "admin",
    password: "secret"
  },

   // Регулярное выражение для проверки формата российского номера телефона
  DEFAULT_PHONE_REGEX: /^\+\7\(\d{3}\)\d{3}-\d{2}-\d{2}$/,
  // Ограничения для длины имени клиента
  MIN_NAME_LENGTH: 3,
  MAX_NAME_LENGTH: 50,
  
  // Время жизни кэша для статистики (в миллисекундах)
  STATS_CACHE_TTL: 60 * 1000,
};
